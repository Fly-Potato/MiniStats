# Repository Guidelines

## 文档分工

本文件面向 AI 与贡献者，规定修改、验证、提交和协作方式。[README.md](README.md) 面向使用者与开发者，维护功能、环境要求、启动命令、目录结构和指标口径。

产品行为或开发入口变化时更新 README；工作规范变化时更新本文件。共同信息通过链接引用，避免维护两份命令和结构清单。不把计划功能写成已实现能力。

## 开始修改前

先阅读 README，检查当前分支、工作区差异及相关源码，保留已有未提交改动。环境和运行命令见 [开发和启动](README.md#开发和启动)，模块位置见 [项目结构](README.md#项目结构)。

## 代码风格与命名

使用四空格缩进；类型使用 `UpperCamelCase`，方法和属性使用 `lowerCamelCase`。遵循现有 Swift 排版，目前没有配置格式化或 lint 工具。

指标优先使用小型值类型，系统采集与 SwiftUI 视图保持分离，可观察 UI 状态在主线程更新。界面文案、README 和贡献说明使用中文。修改指标口径时同步更新文档。

## 测试要求

测试文件命名为 `*Tests.swift`，测试方法以 `test` 开头并描述预期行为，例如 `testCounterResetDoesNotProduceSpike`。目前没有数值化覆盖率门槛。

计算逻辑变更应补充针对性测试，关注采样间隔、计数器重置、网卡增减和缺失数据；不要断言实时 CPU 或网速的固定数值。运行逻辑变更需执行单元测试和 `--sample`；界面变更需手动检查菜单栏与详情面板。分别报告编译、采样和界面验证结果，不能以编译成功代替界面验证。

## Git 提交信息规范

后续人工和 AI 提交统一采用 Conventional Commits 格式，摘要与正文使用中文，技术名称保留原文。既有提交无需重写。

```text
<type>(<scope>): <中文摘要>
```

### 类型与范围

- `feat`：新增功能；`fix`：修复错误或不符合预期的行为。
- `refactor`：不改变外部行为的重构；`perf`：改善性能或降低资源占用。
- `docs`：文档或贡献规则；`test`：仅调整测试。
- `build`：构建、打包、签名或依赖；`ci`：持续集成配置。
- `style`：仅代码格式调整，UI 视觉改动按目的使用 `feat` 或 `fix`。
- `chore`：不属于以上类型的维护工作，不作为默认类型。

类型使用小写英文。范围优先使用 `menubar`、`dashboard`、`metrics`、`monitor`，也可使用明确的小写模块名。跨模块时选择主要范围，没有主范围则省略括号。

### 内容要求

- 完整标题不超过 72 个字符，摘要以具体动作开头，说明行为或结果，不加句号。
- 禁止“优化一下”“更新代码”“修复问题”等模糊描述，不以文件列表代替变更说明。
- 一次提交只围绕一个目的；配套测试和文档可一起提交，无关改动应拆分。
- 简单变更只写标题；复杂变更在空行后说明原因、行为变化和实际验证结果。未执行或未完成的验证必须如实说明。

示例：

```text
feat(menubar): 新增上传下载双行显示
fix(metrics): 修复网速单位切换时出现四位数字
docs: 补充提交信息规范
```

### AI 执行规则

提交前检查暂存区差异，根据实际改动选择类型和范围并生成提交信息。只显式暂存相关文件，排除生成产物与无关改动，检查暂存区空白错误，不虚构测试结果。

提交和推送分别遵循用户授权；“提交并推送”授权两者。不得因修改规范而自行提交、推送或重写历史。目前通过本文件约束，不引入 commitlint 或 Git hooks。

## 合并请求要求

合并请求应说明行为变化、验证方式和已知限制；存在关联问题时附上链接，界面变更附截图。涉及指标定义或支持的网络接口范围时，明确说明变化及其影响。
